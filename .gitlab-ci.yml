pages:
  stage: deploy
  image: python:3.13
  script:
    - pip install -r requirements.txt
    - zensical build --clean
  pages:
    publish: site
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
